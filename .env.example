# Required: your API key from https://the-odds-api.com/
ODDS_API_KEY=

# Optional tuning (CLI env overrides also work):
# MIN_EV=0.03          # minimum EV edge to surface a bet (decimal)
# MAX_BETS=10          # max rows in the ranked slate table
# QUANTSLATE_DATE=     # target date YYYY-MM-DD for fetch_metrics.py (defaults to today)
